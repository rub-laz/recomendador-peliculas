import { Link } from 'react-router-dom';
import BotonAuth from '../components/BotonAuth';

const Home = () => {
  return (
    <div>
      <h1>Bienvenido</h1>
    </div>
  );
};

export default Home;
